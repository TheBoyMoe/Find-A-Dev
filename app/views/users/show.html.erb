<h1><%= @user.name %></h1>
<p><%= @user.email %></p>
<div class="profile-image"><%= image_tag @user.main_image, width: '50%' %></div>
<h3>Bio</h3>
<p><%= @user.bio %></p>

<% if @user.skills.count > 0 %>
	<h3>Technical Skills</h3>
	<dl>
		<% @user.skills.each do |skill| %>
			<dt><%= @user.skills.last.title %></dt>
			<dd><%= @user.skills.last.description %></dd>
	<% end %>
	</dl>
<% end %>

<% if @social_links.count > 0 %>
	<h3>Follow me</h3>
	<% @social_links.each do |link| %>
		<%= link_to @social_links.last.name, @social_links.last.url %>&nbsp;
	<% end %>
<% end %>

<br><br>
<% if current_user == @user %>
	<%= link_to 'Edit Profile', edit_user_path(@user), class: "btn btn-info" %>
<% end %>