<h1><%= current_user.first_name %> conversations</h1>
<% if @conversations.count > 0 %>
	<ul>
		<% @conversations.each do |conversation| %>
			<li><%= link_to conversation.title, conversation_messages_path(conversation) %></li>
		<% end %>
	</ul>
<% end %>